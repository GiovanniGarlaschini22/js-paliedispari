// Generazione numero casuale da 1 a 5 //
function generaNumeroCasuale() {
    return Math.floor(Math.random() * 5) + 1;
}

// Funzione di verifica se numero é pari //
function numPari(numero) {
    return numero % 2 === 0;
}